# Анализ рисков миграции Guest Model

## Статус

✅ **Миграция включена в код** - файл `booking/migrations/0011_add_guest_model.py` существует и будет применена автоматически при `python manage.py migrate`.

## Дата анализа: 2026-01-26

## Обзор изменений

### 1. Новая модель `Guest`
- **Действие**: Создается новая таблица `booking_guest`
- **Влияние**: ✅ **Безопасно** - только добавление, не изменяет существующие данные
- **Размер**: Зависит от количества уникальных имен гостей в БД

### 2. Новое поле `Booking.guest` (ForeignKey)
- **Действие**: Добавляется поле `guest` в таблицу `booking_booking`
- **Влияние**: ✅ **Безопасно** - поле nullable, не требует немедленного заполнения
- **Ограничение**: `on_delete=PROTECT` - защищает от случайного удаления гостей с бронированиями

### 3. Миграция данных `migrate_guest_names_to_guests`
- **Действие**: Читает все `Booking.guest_name`, создает `Guest` записи, связывает `Booking` с `Guest`
- **Влияние**: ⚠️ **Требует внимания** - изменяет данные, но только добавляет связи
- **Риски**:
  - Может быть медленной на большой БД (O(n) по количеству бронирований)
  - Использует `save()` в цикле - может быть оптимизировано
  - Нормализация имен может объединить разные варианты написания

### 4. Поле `guest_name` остается
- **Действие**: Поле помечено как deprecated, но остается в модели
- **Влияние**: ✅ **Безопасно** - обратная совместимость сохранена
- **Статус**: Все существующие views/forms продолжают работать

## Детальный анализ рисков

### 🔴 Критические риски

**Нет критических рисков** - все изменения обратно совместимы.

### 🟡 Средние риски

#### 1. Производительность миграции
- **Проблема**: Миграция использует `save()` в цикле для каждого бронирования
- **Влияние**: На БД с 10,000+ бронированиями может занять несколько минут
- **Решение**: 
  - ✅ Миграция использует `update_fields=['guest']` для оптимизации
  - ✅ Можно запустить в транзакции (по умолчанию Django делает это)
  - ⚠️ Рекомендуется сделать бэкап перед миграцией

#### 2. Нормализация имен
- **Проблема**: Разные варианты написания могут быть объединены в одного гостя
  - Пример: "Иван Иванов" и "ИВАН ИВАНОВ" → один Guest
- **Влияние**: Может быть желаемым поведением (объединение дублей)
- **Решение**: 
  - ✅ Это ожидаемое поведение для борьбы с дублями
  - ✅ Можно вручную разделить через админку после миграции

#### 3. Конфликты при нормализации
- **Проблема**: Если два разных имени нормализуются одинаково
  - Пример: "Иван Иванов" и "Иван Иванов" (с невидимыми символами)
- **Влияние**: Может объединить разных людей
- **Решение**: 
  - ✅ `normalized_name` имеет `unique=True` - Django предотвратит дубликаты
  - ⚠️ Первая запись будет создана, вторая получит существующую
  - ⚠️ Нужно проверить после миграции на наличие неожиданных объединений

### 🟢 Низкие риски

#### 1. Размер БД
- **Влияние**: Добавляется новая таблица и индекс
- **Оценка**: ~100 байт на уникального гостя + индексы
- **Пример**: 1000 уникальных гостей = ~100 KB + индексы (~50 KB)

#### 2. Производительность запросов
- **Влияние**: Добавление JOIN при использовании `guest`
- **Оценка**: Минимальное влияние, индексы созданы
- **Решение**: ✅ Индексы на `normalized_name` и `display_name` созданы

## Функции, изменяющие БД

### `merge_guests()` в `guest_utils.py`
- **Действие**: Объединяет гостей, переносит бронирования, удаляет дубли
- **Использование**: Только через админку или API с подтверждением пользователя
- **Риск**: ⚠️ **Средний** - изменяет данные, но требует явного подтверждения
- **Защита**: 
  - ✅ Использует `@transaction.atomic` для целостности
  - ✅ Требует подтверждения через модальное окно
  - ✅ Логируется через `BookingLog`

## Рекомендации для dev сервера

### Перед применением миграции:

1. **Создать бэкап БД**
   ```bash
   python manage.py dumpdata > backup_before_guest_migration.json
   # или через pg_dump для PostgreSQL
   ```

2. **Проверить количество записей**
   ```python
   python manage.py shell
   >>> from booking.models import Booking, Guest
   >>> print(f"Bookings: {Booking.objects.count()}")
   >>> print(f"Unique guest names: {Booking.objects.values('guest_name').distinct().count()}")
   ```

3. **Протестировать миграцию на копии БД** (если возможно)

### После применения миграции:

1. **Проверить результаты миграции**
   ```python
   python manage.py shell
   >>> from booking.models import Booking, Guest
   >>> print(f"Guests created: {Guest.objects.count()}")
   >>> print(f"Bookings with guest: {Booking.objects.filter(guest__isnull=False).count()}")
   >>> print(f"Bookings without guest: {Booking.objects.filter(guest__isnull=True).count()}")
   ```

2. **Проверить на неожиданные объединения**
   ```python
   # Найти гостей с большим количеством бронирований (возможные объединения)
   >>> from booking.models import Guest
   >>> from django.db.models import Count
   >>> guests_with_many = Guest.objects.annotate(bookings_count=Count('bookings')).filter(bookings_count__gt=10).order_by('-bookings_count')
   >>> for g in guests_with_many[:10]:
   ...     print(f"{g.display_name}: {g.bookings_count} bookings")
   ```

3. **Проверить работу существующих функций**
   - Создание бронирования
   - Редактирование бронирования
   - Отчеты
   - Экспорт данных

## Откат миграции

Если что-то пошло не так, можно откатить миграцию:

```bash
python manage.py migrate booking 0010_bookinglog
```

**⚠️ Внимание**: Откат удалит таблицу `Guest` и поле `Booking.guest`, но:
- ✅ Данные в `guest_name` останутся нетронутыми
- ✅ Все существующие функции продолжат работать
- ⚠️ Нужно будет восстановить бэкап, если были изменения после миграции

## Выводы

### ✅ Безопасно для применения:
- Миграция обратно совместима
- Существующий функционал не ломается
- Данные не удаляются, только добавляются связи
- Можно откатить без потери данных

### ⚠️ Требует внимания:
- Производительность на большой БД (рекомендуется бэкап)
- Проверка результатов нормализации имен
- Тестирование после миграции

### 🎯 Рекомендация:
**Можно безопасно применять на dev сервере** при условии:
1. Создания бэкапа перед миграцией (например `make backup-safe` или через `deploy_safe.sh` — см. [DEPLOYMENT.md](DEPLOYMENT.md))
2. Проверки результатов после миграции
3. Тестирования основных функций

## Дополнительные функции (не влияют на БД при миграции)

- ✅ Автодополнение — только чтение
- ✅ API endpoints — только чтение
- ✅ Поиск дублей — только чтение
- ⚠️ Объединение гостей — изменяет БД, но требует подтверждения

---

См. также: [DEPLOYMENT.md](DEPLOYMENT.md#миграция-guest-model) (раздел «Миграция Guest Model»).
